# Frontend

This directory contains the Next.js frontend for SourceAI.

For the full project overview, setup instructions, and architecture notes, see the root [README.md](../README.md).

Quick start:

```bash
cp .env.example .env.local
npm install
npm run dev
```
